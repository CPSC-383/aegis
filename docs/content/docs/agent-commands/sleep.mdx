---
title: "Sleep"
description: "Represents a command for an agent sleep."
---

## Overview

The `SLEEP` command allows an agent to sleep and recharge energy. For recharging to occur, 
the agent must be on a charging grid when the command is sent.

**Example:**

```py
self._agent.send(SLEEP())
```
