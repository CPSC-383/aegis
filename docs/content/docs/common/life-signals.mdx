---
title: "Life Signals" 
description: "A data structure for organizing and accessing life signal readings."
assignment: "a3"
---

## Overview

The `LifeSignals` class manages a collection of life signals, where each signal represents either
a survivor's energy level (positive integers) or rubble (0). The signal detection system accounts
for depth-based distortion, where signals become more distorted in deeper layers.

## Attributes

<Attribute
  name="life_signals"
  type="list[int]"
  description="A list of life signals."
/>

## Methods

### get(index: int)

<Method
  name="get"
  description="Retrieves the life signal at the specified index."
  parameters={[
    {
      name: "index",
      type: "int",
      description: "The index of the life signal to retrieve.",
      required: true,
    },
  ]}
  returns={{
    type: "int",
    description:
      "The life signal at the specified index.",
  }}
/>

### size()

<Method
  name="size"
  description="Returns the number of life signals."
  returns={{
    type: "int",
    description:
      "The number of life signals.",
  }}
/>
